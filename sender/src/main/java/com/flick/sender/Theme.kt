package com.flick.sender

// The theme moved to com.flick.sender.ui.theme (FlickTheme, FlickColors, Motion,
// Type, Shape) as part of the redesign. This file is intentionally left without a
// FlickTheme declaration so the single source of truth lives under ui/theme/.
