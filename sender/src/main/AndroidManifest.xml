<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required: the sender runs an embedded HTTP server on the LAN and must
         also be reachable over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Used to resolve this device's Wi-Fi/LAN IP to display the cast URL. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- The library is a real MediaStore-backed video gallery, so it needs read
         access to the user's videos. READ_MEDIA_VIDEO (33+) / the partial
         VISUAL_USER_SELECTED (34+) / READ_EXTERNAL_STORAGE (<=32). -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Serving must survive with the screen off while the TV plays, so the
         media server lives in a foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Keeps the CPU servicing the socket threads while the screen is off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the user exempt Flick from OEM battery managers that would
         otherwise kill even a foreground service mid-cast. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- API 33+: the persistent foreground-service notification requires the
         POST_NOTIFICATIONS runtime permission to be visible. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for transport and scrub haptics; normal permission, no prompt. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".FlickApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlickSender">

        <!-- Picker + server-control UI. The activity handles config changes itself
             (Compose re-reads the new Configuration) so rotation / dark-mode / split-
             screen / locale / font-scale never recreate it and tear down the live
             FlickController — the control WS, PlaybackSession, route and casting item
             all survive, mirroring the receiver activity. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|navigation|uiMode|density|fontScale|locale|layoutDirection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="flick" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Foreground service that hosts the Ktor media server for the life of
             a cast session. mediaPlayback type is honoured on API 34+. -->
        <service
            android:name=".CastServerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

    </application>

</manifest>
