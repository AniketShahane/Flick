# Add project-specific ProGuard/R8 rules here.
# Minification is disabled for the Phase 0 spike (see build.gradle.kts), so this
# file is intentionally empty. Module agent adds keep rules if/when needed.
