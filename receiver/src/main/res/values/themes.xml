<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Fullscreen, no-action-bar, black-background theme for the Compose-driven
      leanback Activity. Parented on the platform Material theme (minSdk 26) so
      no AppCompat/Material XML dependency is required — all UI is Compose.
    -->
    <style name="Theme.Flick" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
